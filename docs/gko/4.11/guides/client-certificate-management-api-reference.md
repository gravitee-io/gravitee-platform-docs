# Client Certificate Management API Reference

## Related Changes

The Console UI displays a warning banner when `certificateCount > 1`, indicating "This application has {count} active certificates. The one displayed is the most recently created."
