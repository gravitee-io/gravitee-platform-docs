---
description: What to expect from the API Management Gateway under load before you run APIs on it.
---

# Deploy

Before any API runs on it, the Gateway has to be stood up and sized for the traffic it will carry.

This section covers how the Gateway performs under load, measured under a fixed configuration and tracked from one release to the next.
