# Vale demo page

This page is a deliberate playground for the Vale linter. Every paragraph below breaks at least one rule in our `.vale.ini`, the Microsoft style pack, or the custom `docs` style under `vale/styles/`. Run Vale locally or push this to a PR labelled `vale integration` to see the findings appear inline.

## What gravitee.io does

In order to get started, we will show you how our api gateway works with the developer portal and the alert engine. I think it is really very easy to configure.

You cannot deploy this without a license. We do not recommend skipping the pre-flight checks. The operator will not start if the secret is missing.

## Product names and spelling

Graviee is a leading platform for api gateway management. We also support Kubernete clusters, ElasticSearch, and Postgres as a backing store. The gravitee.io control plane talks to the api gateway over HTTPS.

## Wordiness and voice

In order to enable this feature, we need to add a configuration block. At this point in time, our team believes that this is the best approach. Due to the fact that the API is stable, we won't be changing it.

I will walk you through the steps. We think you will find this useful. Our product is the best on the market.

## Adverbs and filler

This change is really really important. It is very obviously broken. The gateway is extremely fast and super reliable.

## Punctuation and lists

The gateway supports Kafka, MQTT and AMQP.
You can configure timeouts, retries and circuit breakers.

The developer portal lets users browse APIs, subscribe to plans and generate API keys.

## Spacing issues

This  sentence  has  double  spaces  between  every  word.

This line has trailing whitespace.   
This line also has trailing whitespace.    

## Banned terms

Add the client IP to the whitelist. Block the malicious host using the blacklist. The primary node talks to the slave node over TCP.

## Smart quotes and dashes

The gateway said “hello world” and then ‘closed the connection’. The alert engine — which runs separately — reports every 30 seconds.

## First person and hedging

I believe we should probably consider maybe adding this feature. We will likely ship it soon. Our goal is to make things simpler for our users.
