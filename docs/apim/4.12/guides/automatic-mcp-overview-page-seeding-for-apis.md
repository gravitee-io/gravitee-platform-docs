# Automatic MCP Overview Page Seeding for APIs

## Prerequisites

* API must be published to the Portal
* For MCP Proxy APIs: V4 API with `mcp` or `mcp-proxy` entrypoint type configured
* At least one entrypoint URL defined in the API configuration
