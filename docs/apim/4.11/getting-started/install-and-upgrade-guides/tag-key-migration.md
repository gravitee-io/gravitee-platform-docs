# Tag Key Migration: Execution and Troubleshooting

## Prerequisites

Before the automatic migration runs, complete the following steps:

* Apply the database schema update (Liquibase changelog `09_add_tags_key_column.yml`)
* Back up tag data

## Troubleshooting

