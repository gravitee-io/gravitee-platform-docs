# Subscription Form Portal API Reference

## Related Changes

The navigation menu now uses `environment-metadata-*` permissions instead of `environment-settings-*` for subscription form access. The portal page content validation system was refactored from a single inline check to an extensible validator pattern.
