
# Subscription Metadata Integration

## Display

Subscription metadata appears in the Management Console on subscription detail pages. Both the API subscription view and the Application subscription view display a **Metadata** section.

The metadata is displayed using a Monaco JSON editor with the following configuration:

* Read-only mode
* Line numbers disabled
* JSON syntax highlighting
* Word wrap enabled

When no metadata exists, the section displays a dash (`-`).
