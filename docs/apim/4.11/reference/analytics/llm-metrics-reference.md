### Measure Unit Behavior

The `AVG` measure no longer appends a `ms` suffix for non-latency metrics. Computed metrics such as `LLM_PROMPT_TOTAL_TOKEN` (AVG) display raw numeric values without time units.
