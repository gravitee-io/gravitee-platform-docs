# Vale test

Before managing client certificates via REST API, ensure the following:

This request returns resources starting with "prod".

The Agent connects to the management console to allowlisting sharding requests.
