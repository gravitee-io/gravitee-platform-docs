### Overview

Protected Resources represent OAuth 2.0 resource servers that authenticate with the authorization server and validate tokens. This feature extends secret management, certificate binding, membership controls, and token introspection to Protected Resources, enabling secure server-to-server authentication and token exchange flows.
